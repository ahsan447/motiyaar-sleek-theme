{%- liquid
  assign is_external_link = false
  if url contains '://' and url contains request.host == false
    assign is_external_link = true
  endif
-%}
{%- if is_external_link -%}target="_blank" rel="noopener"{%- endif -%}
